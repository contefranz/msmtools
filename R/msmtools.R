#' Building augmented data for multi-state models: the `msmtools` package
#'
#' `msmtools` introduces a fast and general method for restructuring classical longitudinal datasets
#' into *augmented* ones. Augmented data enhances longitudinal datasets and allow to model each
#' transition under a multi-state framework. `msmtools` works in symbiosis with the
#' [msm::msm()] package.
#' It also provides two graphical goodness-of-fit tools to inspect the model performances using
#' survival curves and prevalences under the Markov assumption.
#' `msmtools` comes with 4 functions: `augment()`, `polish()`,
#' `prevplot()`, and `survplot()`.
#'
#' @keywords internal
"_PACKAGE"
